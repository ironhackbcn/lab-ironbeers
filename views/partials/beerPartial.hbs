<div class="beer-card">
    <img class="beer-img" src="{{this.image_url}}" alt="Beer image">
    <div class="beer-body">
        <h3 class="card-title">{{this.name}}</h3>
        <h5 clas="card-tagname">{{this.tagline}}</h5>
        <p class="card-text">{{this.description}}</p>
    </div>
</div>