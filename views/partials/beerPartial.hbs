<div class="beercard">
    <article>
        <image class="imagebeer" src="{{image_url}}" alt="Beer +{{name}}"></image>
        <h2>Name: {{this.name}}</h2>
        <p>Description: {{this.description}}</p>
        <h3>Tag Line: {{this.tagline}}</h3>
    </article>
</div>